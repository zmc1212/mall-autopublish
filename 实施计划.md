# 千牛商品自动录入与入库自动化实施计划

## 目标

基于千牛网页读取商品清单 Excel，逐条录入商品，最终选择“放入仓库”并提交保存。不得选择“立即上架”或定时上架。先完成真实页面字段核验和单商品验证，再扩展批量处理。

## 当前状态

- 模板和本地校验已按中性笔、修正带发布页核验结果更新。
- 网页执行器已接入持久化 Chrome；默认填写后选择放入仓库，不点击提交。
- 还没有真实商品单条入库成功记录，也没有控制面板。

## 一、页面字段核验

工具访问合法恢复前，可依据用户提供的页面截图整理字段，但截图不能替代可运行选择器的验证。不得猜测选择器或将草案字段当作已核验字段。

访问恢复后，只读检查示例商品编辑页，记录字段名称、类型、必填状态、选项值、联动关系、图片要求及 SKU 表结构。重点覆盖：标题、类目、品牌、货号、主图、详情图、商品属性、规格、价格、库存、运费模板、库存扣减方式和上架时间。

必须区分“放入仓库”单选项与实际提交按钮，记录保存成功提示和仓库结果验证方式。示例编辑链接只用于观察，不默认授权覆盖该已有商品。

## 二、Excel 模板与数据校验

- 按核验结果调整模板，区分平台必填、类目条件必填与工具内部标识。
- 商品与 SKU 使用稳定标识关联；支持单规格商品和多规格商品，避免无 SKU 商品也被强制要求 SKU JSON。
- 校验 JSON 类型、SKU 规格组合重复、价格有效且有限、库存为非负整数，以及商品标识重复。
- 校验主图和详情图文件是否存在、是否为文件及格式是否符合页面要求；统一相对路径的基准目录。
- 忽略只有默认状态而没有商品数据的空行。
- 修复日志追加位置，避免因预格式化空行导致日志从第 201 行后开始；明确记录行号、商品标识、失败字段和原因。
- 校验结果与执行结果分开记录，不把校验通过当作入库成功；默认保留输入文件，结果另存。

## 三、网页录入与批量执行

实施前确认新建商品入口及适用类目。不得循环复用带已有 itemId 的编辑链接来创建新商品，以免反复覆盖同一商品。

执行流程：

1. 读取 Excel，先校验整批数据并展示可处理商品与错误。
2. 为每个商品进入经过验证的新建流程，填写基础信息及类目属性。
3. 上传对应图片，填写详情与各 SKU 的规格、价格、库存及商家编码。
4. 核对填写结果，选择“放入仓库”，验证该选项确实选中。
5. 使用经核验的提交按钮保存，读取成功或失败提示。
6. 在仓库中确认对应商品后记录成功及商品标识；无法确认时记录“结果待核实”，不得直接重试提交。

逐条执行，提供进度、暂停和错误日志。普通单商品数据错误可跳过；登录失效、验证码或平台阻断时暂停，由用户通过正常页面处理。恢复时跳过已确认成功的商品，待核实记录先核对，避免重复创建。

本地控制界面后续提供 Excel 选择、校验预览、执行进度和结果查看；在真实自动化能力接通前明确显示未启用状态。

## 四、测试与验收

- 本地校验：覆盖空行、缺列、错误 JSON、重复商品和 SKU、负数、非整数库存、非有限价格、缺失图片及相对路径。
- 日志：覆盖首条记录位置、连续运行、输入文件被占用和结果保存失败；不能丢失已执行结果。
- 单商品：使用用户提供的真实商品数据及图片，验证文本、选择器、属性、图片和 SKU 录入，再验证仓库保存结果。不得用虚构商品数据提交真实店铺。
- 批量：使用 2–3 个真实商品验证逐条执行、失败跳过、暂停恢复和防重复处理。
- 验收：成功商品在仓库可核实，没有商品被立即或定时上架；失败记录可定位具体行与字段；不改动示例已有商品。

## 待补充信息与后续顺序

需要真实类目与字段资料、商品数据、图片素材，以及新建商品入口和浏览器运行方式的核验。

先核验字段并修复本地校验和日志，再确定可用且允许的浏览器集成方式，随后实现单商品入库、批量执行和控制界面。浏览器策略阻塞期间仅推进本地工作，不以其他通道绕过限制。
