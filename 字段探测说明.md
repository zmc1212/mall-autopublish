# 千牛字段探测说明

页面字段已按真实发布页核验，选择器写在 `千牛字段映射.json`。

新建入口：`https://item.upload.taobao.com/sell/ai/category.htm`。确认类目后进入 `/sell/v2/publish.htm?catId=...`。禁止使用带 `itemId` 的编辑页创建商品。

已核验：

- 中性笔 `50012720`：必填属性含品牌、型号、笔头类型等；SKU 常见列是书写粗细、支数、颜色分类。
- 修正带 `121466019`：必填属性为附加功能、风格、可换芯设计、适用场景。
- 上架时间与提交按钮分离：单选项“放入仓库”，按钮“提交宝贝信息”。
- 1:1 主图、3:4 主图、详情图、发货时间、发货地、运费模板。

网页执行器通过 Playwright CLI 连接 `127.0.0.1:9222`（`启动持久化千牛.cmd` 打开的窗口），配置为 `.playwright/remote.config.json`。登录失效、验证码或多规格 SKU 未核对时会暂停，不会反复提交。完整状态见 `开发进展.md`。
