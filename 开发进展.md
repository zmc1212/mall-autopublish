# 开发进展（2026-09-20）

## 自动入库剧本（下一对话从这里续）

完整操作记录在 `testdata/自动化剧本.md`。仍停在 `publish.htm?catId=50012720` 新建页（无 itemId），**放入仓库已选**。**未点提交**。

本轮已完成：

- SKU 五规格：蓝杆/绿杆/白杆/紫杆/黄杆-1支，6.90 元 / 10 件，书写粗细 5 行 `0.05mm`（枚举无精确 0.5mm）
- 运费模板「文具用品 包邮」
- 图片空间授权后已传：五行规格图并一图一规格绑定、1:1 `main-1-1.jpg`、3:4 `main-3-4.jpg`、详情 `detail-01/03/05.jpg`

下一步只等人眼核对后，用户明确说可以再跑 `testdata/submit_warehouse.js`。

# 开发进展（2026-09-19）

## 网页执行器已接入（默认不提交）

- 按已核验的中性笔/修正带字段补齐模板列：3:4主图、导购标题、规格3、发货时间、发货地、SKU明细(JSON)。
- 本地校验增加标题品牌一致性、标题字数、主图1:1、3:4比例、详情图高宽比、中性笔物流必填，并拒绝示例行进入网页执行。
- 新增 `千牛网页执行.py`：调用 Codex 同一套 Node Playwright CLI（`.work/node_modules/playwright-core` + `.playwright/remote.config.json`），连接 `127.0.0.1:9222` 持久化 Chrome。从 `category.htm` 新建商品，填写后只选“放入仓库”。`--submit` 默认不点击“提交宝贝信息”；必须同时加 `--confirm-submit`。
- 拒绝带 `itemId` 的编辑页。登录失效或验证码会暂停。多规格会打开创建规格后暂停，避免自动点提交。
- 尚未用真实商品图片做单条入库验收，也还没有仓库回查和控制面板。

## Playwright 可见窗口启动已修复

- 用户授权改用 Playwright 后，发现含 `&` 的 URL 经 npx.cmd 解析截断，导致后续 headed 参数未生效。启动脚本改用无查询参数的淘宝首页。
- 正确传入 headed 后，默认选择的 `C:/Users/Administrator/AppData/Local/Google/Chrome/Application/chrome.exe` 仍立即退出（exitCode=9）。指定用户正常使用的 `C:/Program Files/Google/Chrome/Application/chrome.exe` 后启动成功；未修改 Chrome 安装。
- `.playwright/cli.config.json` 固定可执行路径和 `headless: false`，使用单独持久化配置目录。`启动千牛浏览器.ps1` 提供重复启动入口。
- 已核实 Windows 存在标题为“千牛工作台 - 商家一站式经营阵地 - Google Chrome”的真实窗口，且 Playwright 可获取标签页和截图。截图：`output/playwright/taobao-visible-login.png`。
- 当前停在千牛登录页，等待用户在新窗口正常登录后继续页面字段核验。未录入或提交商品。

## 页面字段核验完成（未提交）

- 用户已登录；Playwright 复用持久化 profile 后可进入千牛工作台和商品发布页。
- 新建入口为 `https://item.upload.taobao.com/sell/ai/category.htm`，选择类目后进入 `/sell/v2/publish.htm?catId=121466019`。已核验叶子类目：文具用品/文化用品/商务用品>>日常学习用品>>修正带。
- 必填基础字段：1:1主图、宝贝标题、商品属性；该类目必填属性实测为附加功能、风格、可换芯设计、适用场景。品牌为重要提示但页面未标星；还可填环保认证、适用人群、IP联名、修正效果、生产企业、总长度、重量、上市时间。
- 销售信息：销售规格（可创建标准规格）、一口价、总库存；库存扣减方式为拍下减库存/付款减库存；上架时间默认立刻上架，已核验可切换为放入仓库；商品预检仅放入仓库时支持。
- 物流必填：发货时间、发货地、提取方式/使用物流配送；页面要求运费模板。该类目强制七天退货服务承诺。
- 图文描述：3:4主图最多5张、推荐1440x1920以上；1:1主图最多5张、推荐1440x1440以上；详情图高/宽≤2，建议宽度≥1440；视频可选，5秒–5分钟，最多5个；白底图可选，800x800纯白背景。
- 实际提交按钮是“提交宝贝信息”，与“放入仓库”单选项分离。没有填写真实商品、上传图片或提交。

## 中性笔编辑页核验（只读参考商品）

- 按用户指定流程，从“我的商品”页面（一级类目已为文具用品/文化用品/商务用品）列表中点击第一条商品的“编辑商品”，打开商品 `1074400219062` 的编辑页；没有直接导航编辑 URL，也没有修改该商品。
- 真实叶子类目：文具用品/文化用品/商务用品>>笔类/书写工具>>中性笔；类目 ID `50012720`。
- 平台必填属性：笔头类型、笔芯颜色、闭合方式、风格、功能、品牌、型号、适用场景、适用人群、包装方式、采购地。可选/非星标属性观察到上市时间、生产企业、书写长度、是否定制。
- SKU 表的标准列：书写粗细（重要）、支数、颜色分类；另有价格、数量、商家编码、商品条形码、推荐卖点、SKU搜索标题、是否上架等列。参考商品采用“商品规格”单层展示，存在多个 SKU 行。
- 该商品页面显示品牌属性“点石制笔”，但标题含“点石”，页面出现品牌一致性风险提示；自动化必须在提交前校验标题品牌与品牌属性一致，避免流量受限或下架风险。
- 参考商品当前价格示例 115.00/150.00/23.00 元，SKU 数量示例 552、555、541、546、554；仅用于理解表结构，未写入模板或提交。
- 中性笔仍需沿用已核验的 1:1 主图、3:4 主图、详情、物流字段，以及“放入仓库”与“提交宝贝信息”分离规则。

## Playwright 登录状态保持

- `.playwright/cli.config.json` 已使用固定 `G:/workspace/taobao-playwright-profile`，该目录保存 Cookie 和登录状态。
- 新增 `启动持久化千牛.cmd`：独立启动可见 Chrome，并监听 `9222` 远程调试端口。浏览器进程不依赖当前对话或 CLI daemon；对话中断后窗口不会因 Playwright CLI 会话结束而自动关闭。
- 首次运行仍需在该窗口登录；之后继续使用同一 profile。淘宝可能因安全策略、Cookie 过期或异地登录要求重新验证，这属于平台行为，无法由脚本绕过。
- 当前 CLI 配置仍适合一次性自动化；需要长期保持窗口时优先使用上述 `.cmd` 启动器，再通过 CDP 连接。
- 已新增 `.playwright/remote.config.json`，通过 `http://127.0.0.1:9222` 连接独立 Chrome；实测调试端口返回 Chrome 153 的 CDP 信息。

## 本次按计划续开发

- 已再次调用用户指定 Browser Use，运行 `f44d2150-7d00-45ca-8cc2-f356c4bd13de` 返回：只能检测到 `about:blank`，无法访问用户已登录 Chrome。该结果不代表用户没有打开淘宝。
- 本机浏览器连接检查仍返回 `Unable to load browser request-header policy`。本次未进入商品列表、未点击发布商品、未核验字段，未提交商品；不能填写真实选择器或宣称网页验证完成。
- 数值校验改为 Decimal，避免小数库存因 float 舍入被当作整数；新旧 SKU 明细列同时填写时明确报错，避免静默忽略一列。
- 本地回归测试扩展到 11 项并通过。真实页面、模板平台必填规则、网页执行及批量验收仍未完成。
- 下一步需要恢复能访问用户现有登录会话的浏览器工具连接，再继续发布页核验；此处阻塞是工具连接问题，不是店铺授权不足。

## 当前状态

模板和本地校验已按中性笔/修正带页面核验结果更新。网页执行器已接入持久化 Chrome；默认只填写并选择放入仓库，不点击提交。还没有真实商品单条入库成功记录。

验证：`python -m unittest -q test_validation test_web`。本地校验覆盖单/多规格、品牌一致性、示例拦截、图片比例及网页安全规则。

已识别用户的 Chrome ronghai 配置。浏览器工具在标签页枚举、指定URL绑定及浏览器API入口均返回 `Unable to load browser request-header policy`。未读取到淘宝页面，未点击发布商品，未录入或提交任何商品。

## 已实现

- 单规格允许不填 SKU；兼容旧列名 `SKU明细(JSON)*` 和新列名 `SKU明细(JSON)`。
- JSON 类型、规格选项和组合重复、SKU 价格和库存、整批商品标识重复检查。
- 价格要求有限正数，库存要求非负整数；图片检查文件存在性、扩展名和可解码性。
- 图片相对路径统一以输入 Excel 所在目录为基准。
- 忽略仅带处理状态、上架时间及库存扣减方式的空行。
- 输入文件不修改，结果默认生成带时间戳的新 Excel 和 JSON。Excel 保存失败时 JSON 保留校验结果。
- 历史日志压紧后追加，消除预格式化空行导致的第201行起写问题。
- 校验状态和执行状态分开；`--submit` 只填写网页，`--confirm-submit` 才会点击提交宝贝信息。

## 未完成及后续

需要用户提供真实中性笔资料和图片，用 `--submit --limit 1` 做单条填写验收；核对放入仓库选中后再决定是否 `--confirm-submit`。提交后还要在仓库中查询结果，不能把填写成功当成入库成功。

多规格 SKU 表、运费模板下拉、属性枚举联动仍需对着当前页面补完。随后再做批量暂停恢复、防重复执行和控制面板。当前没有真实入库成功记录。
